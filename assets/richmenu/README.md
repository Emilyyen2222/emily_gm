# 圖文選單

`richmenu-icons.png`（線條圖示版，採用中）與 `richmenu.png`（emoji 版，備用）
都是 2500×843，用 `scripts/make-richmenu-image.sh` 以 Chrome headless 渲染 HTML 產生 ——
這樣中文才會用到跟 app 一致的 Huninn 字體。

套用方式：`./scripts/setup-richmenu.sh [圖片路徑]`
